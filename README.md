# CloudFaceRecognitionApplication
**I have this "web-tier" folder inside my EC2 instance**

**login to EC2 instance using the following command: ssh -i "<location of .pem file>" ubuntu@98.84.82.56**

**Then cd into the web-tier folder and run the following command: sudo python3 app.py**

**Now the backend server will be started**

**Now run the workload_generator file to send the requests**